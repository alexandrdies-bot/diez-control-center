# Access and Roles

`diez-control-center` — единое приложение для сотрудников «Диез Имидж».

## Главный принцип

Программа должна работать не как локальная программа на одном ПК, а как клиент к общей системе.

Один пользователь может установить программу на несколько ПК:

* дома;
* на работе;
* на ноутбуке;
* на дополнительном рабочем месте.

Все данные должны храниться в общей базе/API, а не локально внутри конкретного ПК.

## Пользователи и роли

В будущем нужны роли:

* admin;
* manager;
* designer;
* production;
* accountant;
* viewer.

Роли должны управлять доступом к разделам и действиям.

Пример:

* admin: всё;
* manager: заказы, клиенты, статусы, Ozon;
* designer: дизайн-макеты, файлы, задачи;
* production: производство, статусы, материалы;
* accountant: оплаты, счета, закрывающие документы;
* viewer: только просмотр.

## Навигация

Навигация должна быть понятной и не смешивать разные задачи.

Будущие основные разделы:

* Главная панель;
* Заказы;
* Ozon;
* Клиенты;
* Расчёты;
* Товары;
* Производство;
* Оплаты;
* Доставка;
* Интеграции;
* Настройки;
* Пользователи и роли.

## Важно

Текущий экран "Материалы" — временный MVP-экран для проверки связки:

Desktop UI -> API -> PostgreSQL.

Он не является главным смыслом программы.

Главный рабочий экран в будущем — "Заказы".

В будущей навигации экран "Материалы" должен быть перенесён в раздел:

```text
Настройки -> Материалы и цены
```

Материалы и закупочные цены нужны для расчётов, себестоимости и контроля цен.

Это служебный раздел, в первую очередь для admin.

Обычный менеджер не должен случайно менять закупочные цены.

Ozon должен быть отдельным рабочим разделом.

Расчёты должны быть отдельным рабочим разделом.

Заказы сайта должны попадать в приложение.

Будущий чат сайта должен обслуживаться из приложения.

Ozon также должен быть отдельным каналом заказов/сообщений.

Редактирование закупочных цен в будущем должно идти через историю цен и audit log.

## Заказы сайта и чат

Доступ к заказам сайта и сообщениям должен зависеть от роли.

Главный экран будущей ПК-программы — "Заказы".

Заказы с сайта должны создаваться через API, сохраняться в общей базе и отображаться в `diez-control-center`.

Менеджер должен видеть источник заказа: сайт.

Будущий чат сайта должен обслуживаться из приложения.

Сообщения из чата должны отображаться в ПК-приложении.

Ответ сотрудника из ПК-приложения должен возвращаться на сайт через API и общую базу.

Ozon также должен быть отдельным каналом заказов/сообщений.

Материалы остаются служебным разделом настроек.

Ролевые ожидания:

* admin видит всё;
* manager видит заказы и чаты;
* designer может видеть сообщения, связанные с макетами;
* production видит только производственные комментарии, если нужно;
* viewer только просмотр.

## Безопасность

Desktop-приложение не должно хранить пароль PostgreSQL.

Доступ должен идти через API.

В будущем нужны:

* авторизация;
* пользователи;
* роли;
* права доступа;
* audit log;
* история действий.
