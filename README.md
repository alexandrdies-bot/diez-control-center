# Диез Имидж Control Center

Главная ПК-программа / внутренний центр управления экосистемой «Диез Имидж».

## Назначение

`diez-control-center` — это не справочник материалов, а единый головной центр управления экосистемой «Диез Имидж».

Главное назначение программы:

- приём и обработка заказов;
- управление заказами с сайта;
- будущая интеграция с Ozon;
- управление товарами для сайта и Ozon;
- расчёты стоимости и себестоимости;
- управление материалами и закупочными ценами;
- клиенты и контакты;
- оплаты;
- доставка;
- производственные статусы;
- интеграции;
- настройки.

Главный рабочий экран в будущем должен быть "Заказы".

Ozon должен быть отдельным будущим модулем.

Расчёты должны быть отдельным будущим модулем.

Заказы сайта должны попадать в приложение через общую связку:

```text
diez-site -> API -> diez-data-core -> diez-control-center
```

Будущий чат сайта должен обслуживаться из приложения.

Ozon также должен быть отдельным каналом заказов/сообщений.

Подробнее: [`docs/SITE_ORDERS_AND_CHAT.md`](docs/SITE_ORDERS_AND_CHAT.md).

Draft будущей модели заказов и чата описан в [`docs/FUTURE_ORDERS_AND_CHAT_MODEL.md`](docs/FUTURE_ORDERS_AND_CHAT_MODEL.md).

Документ фиксирует будущие сущности заказов, клиентов, диалогов, сообщений, ролей и прав без создания миграций.

## Временный экран "Материалы"

Текущий экран "Материалы" — временный технический MVP-экран.

Он нужен только для проверки связки:

```text
Desktop UI -> API -> PostgreSQL
```

Материалы в будущем будут внутренним справочником для расчётов, а не главным назначением программы.

В будущей навигации экран должен быть перенесён в раздел:

```text
Настройки -> Материалы и цены
```

Материалы и закупочные цены нужны для расчётов, себестоимости и контроля цен.

Это служебный раздел, в первую очередь для admin.

Обычный менеджер не должен случайно менять закупочные цены.

Главный рабочий раздел программы в будущем — "Заказы".

Ozon должен быть отдельным рабочим разделом.

Расчёты должны быть отдельным рабочим разделом.

Редактирование закупочных цен в будущем должно идти через историю цен и audit log.

Текущий MVP остаётся read-only и безопасным.

## Главное правило

Одна бизнес-система — одна общая база данных.

Центральная база данных находится здесь:

```text
D:\_ProjectHome\diez-data-core
```

Сайт находится здесь:

```text
D:\_ProjectHome\diez-site
```

Этот проект не должен создавать отдельную независимую базу данных и не должен дублировать таблицы заказов, клиентов, товаров, цен, доставок или оплат.

## Архитектурное решение

Desktop:

```text
Tauri 2 + React + TypeScript + Vite
```

API:

```text
Node.js + TypeScript + Fastify
```

База данных:

```text
PostgreSQL из D:\_ProjectHome\diez-data-core
```

Правильный доступ к данным:

```text
Desktop UI -> API -> PostgreSQL
```

DB-секреты, токены доставок, ключи оплат и другие чувствительные данные нельзя хранить в desktop-клиенте.

## Доступ, роли и навигация

`diez-control-center` должен работать не как локальная программа на одном ПК, а как клиент к общей системе.

Один пользователь сможет установить программу на несколько ПК: дома, на работе, на ноутбуке или на дополнительном рабочем месте.

Все данные должны храниться в общей базе/API, а не локально внутри конкретного ПК.

Будущие роли: admin, manager, designer, production, accountant, viewer.

Будущая модель навигации описана в [`docs/NAVIGATION_MODEL.md`](docs/NAVIGATION_MODEL.md).

Будущие основные разделы: Главная панель, Заказы, Чат / Сообщения, Ozon, Клиенты, Расчёты, Производство, Оплаты, Доставка, Товары, Настройки.

Внутри настроек: Материалы и закупочные цены, Пользователи и роли, Интеграции, Способы оплаты, Способы доставки, Справочники, Системные настройки.

Материалы и цены должны быть не главным рабочим разделом, а служебным подразделом `Настройки -> Материалы и цены`.

Доступ должен идти через API. Desktop-приложение не должно хранить пароль PostgreSQL.

Подробнее: [`docs/ACCESS_AND_ROLES.md`](docs/ACCESS_AND_ROLES.md).

## Структура проекта

```text
D:\_ProjectHome\diez-control-center
  apps
    desktop
    api
  packages
    shared
  docs
  outputs
```

## Работа с Codex

Правила работы с Codex описаны в [`docs/CODEX_WORKFLOW.md`](docs/CODEX_WORKFLOW.md).

В этом файле также зафиксировано правило рекомендуемого уровня интеллекта Codex для задач.

## Границы текущего каркаса

На этом этапе проект содержит только начальную структуру и документацию.

Сейчас намеренно не создаются:

- рабочее Tauri-приложение;
- рабочий Fastify API;
- package.json и lock-файлы;
- зависимости;
- подключение к PostgreSQL;
- миграции;
- CRUD по заказам, клиентам, оплатам или доставке.

## База и миграции

Все будущие изменения структуры общей базы должны проектироваться и выполняться в `D:\_ProjectHome\diez-data-core`.

В `diez-control-center` нельзя создавать собственные миграции, которые расходятся с общей базой экосистемы.

## MVP-направление

Безопасный первый MVP для ПК-программы — read-only просмотр данных, которые уже есть в общей базе:

- единицы измерения;
- материалы;
- specs материалов;
- закупочные pricing inputs;
- товары;
- услуги;
- specs световых букв.

Заказы, клиенты, оплаты и доставки требуют будущего проектирования схемы в `diez-data-core` и не должны добавляться точечно в этот проект без общей модели.
